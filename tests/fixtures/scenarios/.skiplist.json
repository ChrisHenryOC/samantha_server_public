{
  "_comment": "Scenarios excluded from the included_accuracy gate due to known upstream/design issues. Each entry is a tracked GH issue. When the underlying issue is fixed, remove the scenario_id from this list and re-run replay.",
  "skipped": {}
}
